import React from "react";
import { Heading, Slide } from "spectacle";

export const MultiThreading = () => (
  <Slide transition={["fade"]} bgColor="primary" textColor="tertiary">
    <Heading size={6} textColor="secondary" caps>
      Standard Multi threading
    </Heading>
    that's me -> Multi-Threading Backery which sells this hey'yo one request
    from my side -> one baker cames to serve even hold stuff for a session/or
    forget everything multi - doing the same some could prepare big orders or
    fancy cakes which takes a while maybe I need to wait, maybe not
  </Slide>
);
