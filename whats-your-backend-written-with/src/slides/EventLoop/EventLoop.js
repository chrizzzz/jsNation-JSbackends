import React from "react";
import { Heading, Slide } from "spectacle";

export const EventLoop = () => (
  <Slide transition={["fade"]} bgColor="secondary" textColor="primary">
    <Heading>Event Loop, one to create X stuff</Heading>
    that's me -> Event Loop Backery, with just one backer which sells this
    hey'yo one request from my side -> backer put it into a queue backer takes
    item's from the different queue at some point bakery is done with my List
  </Slide>
);
